class_name EnemyDamageReceiver
extends DamageReceiver
#
#enum HitType { NORMAL, KNOCKDOWN, POWER }
#
#signal enemy_damage_receiver(damage: int, direction: Vector2, hit_type: HitType)
#
#func emit_damage(damage: int, direction: Vector2, hit_type: HitType) -> void:
	#enemy_damage_receiver.emit(damage, direction, hit_type)
